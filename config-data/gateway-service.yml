server:
  port: 8099

eureka:
  client:
    serviceUrl:
      defaultZone: http://209.145.60.40:8761/eureka/

spring:
  cloud:
    gateway:
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
      globalcors:
        corsConfigurations:
          '[/**]':
              allowedOrigins: "*"
              allowedMethods: "*"
              allowedHeaders: "*"
      discovery:
        locator:
          enabled: true
      routes:
        - id: persona-service
          uri: https://b373-190-232-157-190.ngrok.io/
          predicates:
            - Path=/persona/**
        - id: solicitud-service
          uri: lb://solicitud-service
          predicates:
            - Path=/solicitud/**